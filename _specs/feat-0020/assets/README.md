# feat-0020 reference assets

This feature has **no pixel design screenshot**. Mobile behavior is defined against:

- Current implementation audit (`header.tsx`, `ImiliFooter.tsx`, `app/page.tsx` + homepage sections)
- Industry-standard patterns (touch targets, drawer nav, responsive type, carousel peek)
- QA viewports in [PRODUCT.md](../PRODUCT.md#qa-viewports)

Use browser DevTools device mode and manual tap testing — not image overlay.
