# feat-0025 assets

No design reference images for this feat.

Social icons use existing `lucide-react` and `@phosphor-icons/react` (TikTok) — no new raster assets required.

**Layout QA:** verify six-icon wrap @ 375px on both `ForestFooter` and `ImiliFooter` in browser DevTools — no screenshot baseline in this folder.
