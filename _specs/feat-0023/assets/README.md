# feat-0023 assets

No reference screenshots for this spec — motion is behavioral, not pixel-layout.

## QA method

1. Record screen captures or use browser DevTools **Rendering → Emulate prefers-reduced-motion** for A/B.
2. Compare before/after screen recordings @ **1440×900** and **375×812** on `/`.
3. Optional: add short `.mp4` clips to this folder after sign-off (`homepage-motion-desktop.mp4`, `homepage-motion-mobile.mp4`).
