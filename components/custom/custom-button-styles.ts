/** Shared arrow icon sizing for CustomButton and custom icon overrides. */
export const customButtonIconClassName = "size-5 shrink-0";

/** Square circular arrow control (article cards, etc.). */
export const arrowCircleButtonClassName =
  "flex size-11 shrink-0 items-center justify-center rounded-full";
